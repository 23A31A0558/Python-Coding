# multiply=lambda x,y:x*y
# print(multiply(5, 3))

# a=[1, 2, 3, 4, 5]
# double=lambda x:x*2
# result=map(double, a)
# print(list(result))

# a=[1, 2, 3, 4, 5]
# result=map(lambda x:x*2, a)
# print(list(result))